# Consegna B

Completa solo `PostCard.vue`. Non spostare lo state nel child e non introdurre `fetch`.

Definition of done:

- `npm run build` passa;
- Like emette `toggle-like` con `post.id`;
- Delete emette `delete` con `post.id`;
- Delete non compare quando `canDelete=false`;
- nessuna mutazione diretta di `post`.
