# Note docente

Far osservare prima Network/curl/TestClient e solo dopo Swagger UI. `/docs` e una vista del contratto, `/openapi.json` e la representation macchina.

Insistere sul 422: non e automaticamente giusto o sbagliato. E un default FastAPI che puo essere compatibile o incompatibile con un contratto preesistente.

Non introdurre SQLAlchemy o dependency injection avanzata in questo microscope.
