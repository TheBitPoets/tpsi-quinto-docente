# Runtime config contract

Il programma legge `{ "env": { ... } }` da stdin e stampa un solo JSON compatto. Implementa validation environment e fail-fast production senza importare FastAPI/Uvicorn/SQLAlchemy.
