# Note docente

Questa milestone e deliberatamente feature-neutral: il prodotto non cambia. La review deve chiedere quale property copre ogni file di test. Far eseguire `pytest -q tests/test_isolation.py` anche con ordine invertito. Il repository integration test usa SQLite reale; il restart test possiede due app/Engine distinti.
