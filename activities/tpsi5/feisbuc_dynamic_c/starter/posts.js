export function createPost(author, text) {
  // TODO: restituisci un nuovo post con:
  // id stabile, author, text, likes: 0, liked: false.
  // Usa crypto.randomUUID() per l'id nel browser.
  return {};
}

export function toggleLike(posts, targetId) {
  // TODO: riusa il modello dell'Activity B:
  // map + object spread, senza mutare direttamente il post target.
  return posts;
}
