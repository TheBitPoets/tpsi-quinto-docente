# Activity D — debug dello stato SQL

Lo script e volutamente rotto.

## Metodo obbligatorio

1. leggi schema e seed;
2. prevedi il primo errore prima di eseguire;
3. compila `DIAGNOSI.md`;
4. correggi una causa alla volta;
5. riesegui;
6. verifica lo stato finale con una `SELECT`.

Stato finale richiesto:

```text
p1  likes=2 liked=1
p2  likes=1 liked=1
p3  likes=0 liked=0
```

`remove-me` deve essere eliminato.

Non e consentito rimuovere i constraint per far passare lo script.
