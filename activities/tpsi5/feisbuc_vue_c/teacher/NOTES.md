# Note docente

Review in tre colonne: reattivita, component contract, HTTP. Non accettare soluzioni che aggirano auth leggendo cookie/token o che spostano `fetch` dentro PostCard.

La CI compone il backend auth milestone 7 con `dist/` della solution Vue in `public/vue`, poi verifica che SPA statica e API convivano sullo stesso origin. Non equivale a browser autograding dello studente.
