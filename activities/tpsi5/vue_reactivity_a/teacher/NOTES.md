# Note docente

Far eseguire prima l'app senza spiegare `computed`. Chiedere agli studenti di prevedere il comportamento, poi mostrare `.value` nello script e l'unwrapping nel template.

Gate docente: `npm install && npm run build` deve passare. Non e autograding della consegna browser.
