# Osservazioni

1. Dove serve `.value` e dove non compare?
2. Che cosa succede a `doubled` quando cambia `count`?
3. Perche `doubled` non e un secondo `ref` aggiornato dentro `increment()`?
4. Che cosa astrae `v-model.number` rispetto a `input.value` + evento?
5. Riscrivi in pseudocodice il ciclo equivalente con DOM manuale.
